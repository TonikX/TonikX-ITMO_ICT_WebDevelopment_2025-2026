# Задание 3 — простой HTTP-сервер, отдающий index.html

Сервер принимает TCP-подключение, читает HTTP-запрос и отдаёт HTML из файла `index.html`.

## Запуск
```bash
python server.py
```

Открыть в браузере:
- http://127.0.0.1:8080/
