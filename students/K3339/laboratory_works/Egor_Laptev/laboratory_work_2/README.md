# Racing Portal - Система управления автогонками

Веб-приложение для управления автогонками, регистрации участников, просмотра результатов и комментирования гонок.

## Возможности

- 📊 **Табло победителей** - отображение результатов гонок с полной информацией об участниках
- 🏁 **Регистрация на гонки** - пользователи могут регистрироваться на гонки со своими автомобилями
- ✏️ **Управление регистрациями** - редактирование и удаление своих регистраций
- 💬 **Комментарии и отзывы** - система комментариев с рейтингом и типами
- 👤 **Профили участников** - управление профилем, автомобилями и регистрациями
- 🔧 **Админ-панель** - управление гонками, результатами и участниками через Django Admin

## Быстрый старт

### Требования

- Python 3.8+
- Docker и Docker Compose
- pip

### Установка и запуск

1. **Клонируйте репозиторий** (если нужно)

2. **Установите зависимости Python**

```bash
pip install -r requirements.txt
```

3. **Запустите PostgreSQL в Docker**

```bash
docker-compose up -d
```

4. **Примените миграции базы данных**

```bash
python manage.py migrate
```

5. **Создайте суперпользователя** (опционально)

```bash
python manage.py createsuperuser
```

6. **Запустите сервер разработки**

```bash
python manage.py runserver
```

7. **Откройте в браузере**

- Приложение: http://127.0.0.1:8000/
- Админ-панель: http://127.0.0.1:8000/admin/

## Структура проекта

```
.
├── app/                    # Настройки Django проекта
│   ├── settings.py         # Настройки приложения (включая БД)
│   └── urls.py             # Главный URL конфиг
├── racing/                 # Основное приложение
│   ├── models.py           # Модели данных
│   ├── views.py            # Представления
│   ├── forms.py            # Формы
│   ├── admin.py            # Настройки админ-панели
│   ├── urls.py             # URL маршруты
│   └── templates/          # HTML шаблоны
├── docker-compose.yml      # Конфигурация Docker для PostgreSQL
├── requirements.txt        # Python зависимости
└── manage.py              # Django management скрипт
```

## Документация

- [DOCKER_SETUP.md](DOCKER_SETUP.md) - Подробная инструкция по работе с Docker
- [POSTGRESQL_SETUP.md](POSTGRESQL_SETUP.md) - Настройка PostgreSQL (с Docker и без)

## Управление Docker контейнером

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f db

# Перезапуск
docker-compose restart db
```

## Основные команды Django

```bash
# Применить миграции
python manage.py migrate

# Создать миграции
python manage.py makemigrations

# Создать суперпользователя
python manage.py createsuperuser

# Запустить сервер
python manage.py runserver

# Подключиться к базе данных
python manage.py dbshell
```

## Параметры подключения к БД

По умолчанию (при использовании Docker):

- **Host:** localhost
- **Port:** 5432
- **Database:** racing_db
- **User:** postgres
- **Password:** postgres

Для изменения параметров отредактируйте `docker-compose.yml` и `app/settings.py` или используйте переменные окружения.

## Технологии

- **Backend:** Django 5.2.8
- **Database:** PostgreSQL 15
- **Frontend:** HTML, CSS (Tailwind), Bootstrap
- **Containerization:** Docker, Docker Compose

## Лицензия

Проект создан в учебных целях.

