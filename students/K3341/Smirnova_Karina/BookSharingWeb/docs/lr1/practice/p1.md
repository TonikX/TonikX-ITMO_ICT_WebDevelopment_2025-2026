# Практика 1.1. Создание базового приложения на FastAPI

## 1. Установка и запуск приложения

Приложение было установлено командой: `pip install fastapi[all]`

В `main.py` файле был создан эндпойн, который возвращает строку:
```python
from fastapi import FastAPI

app = FastAPI()


@app.get("/")
def hallo():
    return {"message": "Hallo!"}
```

Для запуска использовалась конструкция:
```python
if __name__=="__main__":
    uvicorn.run("main:app", reload=True)
```

## 2. Создание виртуальной БД

В `main.py` был создан объект, имитирующий базу данных со следующим содержимым:
```python
temp_db = [{
    "id": 1,
    "owner": {
        "id": 1,
        "username": "Dan",
        "email": "dan@mail.ru",
        "password": "password",
        "created_at": "2026-03-10 12:16:00",
    },
    "request": {
        "id": 2,
        "username": "Pen",
        "email": "pen@mail.ru",
        "password": "password",
        "created_at": "2026-03-11 12:16:00",
    },
    "book": {
        "id": 1,
        "user_id": 1,
        "title": "Peace and war",
        "author": "Tolstoy",
        "language": "Rus",
        "status": "ACTIVE",
        "condition": "NEW",
        "created_at": "2026-03-12 12:16:00",
    },
    "status": "ACTIVE",
    "created_at": "2026-03-15 12:16:00",
    "updated_at": "2026-03-15 12:16:00"
    },
    {
    "id": 2,
    "owner": {
        "id": 2,
        "username": "Pen",
        "email": "pen@mail.ru",
        "password": "password",
        "created_at": "2026-03-11 12:16:00"
    },
    "request": {
        "id": 1,
        "username": "Dan",
        "email": "dan@mail.ru",
        "password": "password",
        "created_at": "2026-03-10 12:16:00",
    },
    "book": {
        "id": 2,
        "user_id": 2,
        "title": "Jane Air",
        "author": "Bronte",
        "language": "Eng",
        "status": "ACTIVE",
        "condition": "NEW",
        "created_at": "2026-03-12 12:16:00",
    },
    "status": "FINISHED",
    "created_at": "2026-03-13 12:16:00",
    "updated_at": "2026-03-15 12:16:00"
    }]
```

Из этой переменной будут браться данные для реализации базового CRUD-интерфейса.

## 3. Добавление Pydantic, Аннотирование и документирование

Для каждого типа объекта БД была реализована собственная Pydantic-модель в файле `models.py`.
```python
from datetime import datetime
from enum import Enum
from typing import Optional
from pydantic import BaseModel, Field, EmailStr, HttpUrl

class User(BaseModel):
    id: int
    username: str
    email: EmailStr
    password: str
    created_at: datetime

class Profile(BaseModel):
    id: int
    user: User
    age: Optional[int] = Field(None, ge=7, le=100)
    address: Optional[str] = None
    description: Optional[str] = None

class Status(Enum):
    active = "ACTIVE"
    sold = "SOLD"
    booked = "BOOKED"

class Condition(Enum):
    new = "NEW"
    good = "GOOD"
    satisfactory = "SATISFACTORY"
    bad = "BAD"

class DealSatus(Enum):
    active = "ACTIVE"
    finished = "FINISHED"

class Book(BaseModel):
    id: int
    user_id: int
    title: str = Field(min_length=1, max_length=200)
    author: str = Field(min_length=1, max_length=100)
    photo: Optional[HttpUrl] = None
    description: Optional[str] = None
    genre: Optional[str] = None
    language: Optional[str] = None
    status: Status
    condition: Condition
    created_at: datetime

class Deal(BaseModel):
    id: int
    owner: User
    request: User
    book: Book
    status: DealSatus = "ACTIVE"
    created_at: datetime
    updated_at: Optional[datetime] = None

class Chat(BaseModel):
    id: int
    title: str = Field(min_length=1, max_length=200)
    created_at: datetime

class Message(BaseModel):
    id: int
    chat: Chat
    sender: User
    content: str
    created_at: datetime

class UserChat(BaseModel):
    id: int
    user: User
    chat: Chat
    created_at: datetime
```

## 4. Создание АПИ-эндпоинтов

Для реализации CRUD-API в базовой имплементации приложения использовались специальные методы, описывающие 
HTTP-запросы внутри веб-приложения. Все подобные методы вызываются через переменную реализации класса FastAPI.
Я реализовала следующие эндпоинты для сущности сделки в файле `main.py`:

```python
@app.get("/deal_list")
def deal_list() -> List[Deal]:
    return temp_db

@app.get("/deal/{deal_id}")
def deal_by_id(deal_id: int) -> List[Deal]:
    return [deal for deal in temp_db if deal.get("id") == deal_id]

@app.post("/deal")
def create_deal(deal: dict) -> TypedDict('Response', {"status": int, "data": Deal}):
    temp_db.append(deal)
    return {"status": 200, "data": deal}

@app.delete("/deal/{deal_id}")
def delete_deal(deal_id: int):
    for i, deal in enumerate(temp_db):
        if deal.get("id") == deal_id:
            temp_db.pop(i)
            break
    return {"status": 201, "message": "delete"}

@app.put("/deal/{deal_id}")
def delete_deal(deal_id: int, deal: dict) -> List[Deal]:
    for i, deal in enumerate(temp_db):
        if deal.get("id") == deal_id:
            temp_db[i] = deal
    return temp_db
```

## 5. Добавление API и данных для вложенного объекта Books

По условию задания в файл `main.py` был добавлен объект, хранящий книги и пользователей:

```python
books_db = [
    {
        "id": 1,
        "user_id": 1,
        "title": "Война и мир",
        "author": "Лев Толстой",
        "photo": None,
        "description": "Эпопея о жизни русского общества в эпоху наполеоновских войн",
        "genre": "Roman",
        "language": "RUSSIAN",
        "status": "ACTIVE",
        "condition": "GOOD",
        "created_at": "2026-03-10T12:00:00"
    },
    {
        "id": 2,
        "user_id": 2,
        "title": "Jane Eyre",
        "author": "Charlotte Bronte",
        "photo": "https://example.com/jane-eyre.jpg",
        "description": "A classic novel about love and independence",
        "genre": "Roman",
        "language": "ENGLISH",
        "status": "ACTIVE",
        "condition": "NEW",
        "created_at": "2026-03-11T12:00:00"
    },
    {
        "id": 3,
        "user_id": 1,
        "title": "Краткая история времени",
        "author": "Стивен Хокинг",
        "photo": None,
        "description": "О происхождении Вселенной",
        "genre": "SCIENCE",
        "language": "RUSSIAN",
        "status": "BOOKED",
        "condition": "SATISFACTORY",
        "created_at": "2026-03-12T12:00:00"
    }
]

users_db = [
    {"id": 1, "username": "dan", "email": "dan@mail.com"},
    {"id": 2, "username": "pen", "email": "pen@mail.com"},
]
```

Также были добавлены эндпоинты для работы с книгами:

```python
@app.get("/book_list")
def book_list() -> List[Book]:
    return books_db


@app.get("/book/{book_id}")
def book_by_id(book_id: int) -> List[Book]:
    return [book for book in books_db if book.get("id") == book_id]


@app.post("/book")
def create_book(book: Book) -> TypedDict('Response', {"status": int, "data": Book}):
    new_id = max([b.get("id") for b in books_db], default=0) + 1
    book_to_append = book.model_dump()
    book_to_append["id"] = new_id
    books_db.append(book_to_append)
    return {"status": 200, "data": book}


@app.delete("/book/{book_id}")
def delete_book(book_id: int):
    for i, book in enumerate(books_db):
        if book.get("id") == book_id:
            books_db.pop(i)
            break
    return {"status": 201, "message": "delete"}


@app.put("/book/{book_id}")
def update_book(book_id: int, book: Book) -> List[Book]:
    for l in books_db:
        if l.get("id") == book_id:
            book_to_append = book.model_dump()
            temp_db.remove(l)
            temp_db.append(book_to_append)
    return books_db
```