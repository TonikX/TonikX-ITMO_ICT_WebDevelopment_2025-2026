# ЛР1 — Задание 1

## Как запустить и проверить
1. В одном терминале запустите сервер:
   ```bash
   python udp_server.py
   ```
2. Во втором терминале запустите клиента:
   ```bash
   python udp_client.py
   ```

**Ожидаемый результат**
- На сервере появится строка вида:
  ```
  Получено от ('127.0.0.1', <порт_клиента>): Hello, server
  ```
  ![img_1.png](img_1.png)

- На клиенте:
  ```
  Ответ от сервера: Hello, client
  ```
  ![img.png](img.png)

## Выводы

В ходе выполнения работы я:

- Реализовал клиентскую и серверную часть приложения с использованием **UDP-сокетов** в Python.
- Научился работать с функциями `socket()`, `sendto()` и `recvfrom()`, использовать адреса и порты для обмена данными.
- Разобрался в принципе работы протокола **UDP** — как он передаёт сообщения без установления соединения.
- Освоил базовый цикл приёма и отправки сообщений между процессами.
- Проверил корректность обмена сообщениями: клиент успешно получает ответ от сервера.

Таким образом, получил практические навыки создания простых сетевых приложений и понял разницу между протоколами транспортного уровня.
ма демонстрирует минимальный пример клиент-серверного обмена сообщениями через UDP.