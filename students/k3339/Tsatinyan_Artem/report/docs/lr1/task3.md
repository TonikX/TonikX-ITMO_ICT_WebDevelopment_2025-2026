# ЛР1 — Задание 3 (HTTP): Отдача HTML-файла по сокетам

**Задача:** Реализовать сервер, который принимает подключение от клиента и возвращает **HTTP-сообщение**, содержащее HTML-страницу, подгружаемую из файла `index.html`.

## Как запустить
1. Убедитесь, что рядом с сервером лежит `index.html` (пример ниже).
2. Запустите HTTP-сервер:
   ```bash
   python http_server_task3.py
   ```
3. Откройте в браузере:
   - http://127.0.0.1:8080/
   - http://127.0.0.1:8080/index.html

**Если файла `index.html` нет**, сервер вернёт `404 Not Found`.

![img_5.png](img_5.png)

![img_6.png](img_6.png)

## Выводы

В ходе выполнения задания я:

- Реализовал простой **HTTP-сервер на сокетах**, который отдаёт HTML-страницу при обращении по адресу `/` или `/index.html`.
- Научился вручную формировать **HTTP-ответ**: строку статуса, заголовки (`Content-Type`, `Content-Length`, `Connection`) и тело ответа.
- Освоил принципы работы **HTTP/1.1** — структуру запроса и ответа, разделение заголовков и контента.
- Разобрался, как браузер взаимодействует с сервером по TCP и как можно отдавать статические файлы без использования фреймворков.
- Реализовал обработку ошибок: при запросе неизвестного пути возвращается **404 Not Found**.
- Проверил работу через браузер и убедился, что HTML-файл корректно передаётся клиенту.

Таким образом, я получил практические навыки работы с HTTP-протоколом на низком уровне и понял, как формируются веб-страницы на стороне сервера.
