# Лабораторная работа №4. Реализация клиентской части средствами Vue.js
***
**Автор:** Машковцева Марина
***
## 10 Вариант:
Создать программную систему, предназначенную для администратора лечебной
клиники.

Прием пациентов ведут несколько врачей различных специализаций. На каждого
пациента клиники заводится медицинская карта, в которой отражается вся
информация по личным данным больного и истории его заболеваний (диагнозы). При
очередном посещении врача в карте отражается дата и время приема, диагноз, текущее
состояние больного, рекомендации по лечению. Так как прием ведется только на
коммерческой основе, после очередного посещения пациент должен оплатить
медицинские услуги (каждый прием оплачивается отдельно). Расчет стоимости
посещения определяется врачом согласно прейскуранту по клинике.

Для ведения внутренней отчетности необходима следующая информация о врач:
фамилия, имя, отчество, специальность, образование, пол, дата рождения и дата начала
и окончания работы в клинике, данные по трудовому договору. Для каждого врача
составляется график работы с указанием рабочих и выходных дней.

Прием пациентов врачи могут вести в разных кабинетах. Каждый кабинет имеет
определенный режим работы, ответственного и внутренний телефон.

## Реализация проекта:
1. Настройка CORS для серверной части (ЛР №3)
✅ Выполнено:

- Установлен и настроен пакет django-cors-headers в Django проекте

- Добавлены следующие настройки в settings.py:

```python
INSTALLED_APPS = [
    ...
    'corsheaders',
]

MIDDLEWARE = [
    ...
    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.common.CommonMiddleware',
    ...
]

CORS_ALLOW_ALL_ORIGINS = True 

CORS_ALLOW_METHODS = [
    'DELETE',
    'GET',
    'OPTIONS',
    'PATCH',
    'POST',
    'PUT',
]

CORS_ALLOW_HEADERS = [
    'accept',
    'accept-encoding',
    'authorization',
    'content-type',
    'dnt',
    'origin',
    'user-agent',
    'x-csrftoken',
    'x-requested-with',
]
```
- Протестирована кросс-доменная работа между фронтендом (Vue.js на порту 8080) и бэкендом (Django на порту 8000)

2. Утверждение интерфейсов взаимодействия
### Основные модели и API endpoints:
- Пациенты (/api/patients/)

    - CRUD операции для управления пациентами

    - Автоматическое создание медицинской карты при создании пациента

- Врачи (/api/doctors/)

    - CRUD операции для управления врачами

    - Поля: ФИО, специальность, образование, даты работы, договорная информация

- Кабинеты (/api/rooms/)

    - CRUD операции для управления кабинетами

    - Поля: номер кабинета, время работы, ответственный, телефон

- Визиты (/api/visits/)

    - CRUD операции для записи на прием

    - Связь с пациентом, врачом и кабинетом

    - Поля: диагноз, состояние пациента, рекомендации, стоимость, статус оплаты

- Платежи (/api/payments/)

    - Создание платежей за визиты

    - Автоматическое обновление статуса оплаты визита

    - Поддержка способов оплаты: наличные/карта

3. Реализация интерфейсов авторизации
✅ Выполнено:

- Настроена аутентификация через Django REST Framework (DRF)

- Реализованы следующие endpoints:

    - /api/auth/login/ - вход в систему
    
    - /api/auth/register/ - регистрация пользователя
    
    - /api/auth/logout/ - выход из системы
    
    - /api/auth/user/ - получение/изменение данных пользователя

- Использованы стандартные DRF компоненты:

    - TokenAuthentication для API авторизации

    - SessionAuthentication для веб-интерфейса

- Настроены разрешения (permissions) для разных ролей пользователей

4. Реализация клиентских интерфейсов (Vue.js)
✅ Выполнено:

- Создана SPA-архитектура с Vue Router

- Реализованы следующие основные страницы:

### 4.1 Пациенты (/patients)
- Форма добавления/редактирования пациента со всеми полями

- Список пациентов с возможностью редактирования и удаления

- Автоматическое создание медицинской карты

### 4.2 Врачи (/doctors)
- Полная форма с обязательными и дополнительными полями

- Список врачей с кнопками редактирования и удаления

- Отображение специальности и дат работы

### 4.3 Кабинеты (/rooms)
- Форма для добавления кабинетов с временем работы

- Список кабинетов с информацией об ответственном лице

- Редактирование и удаление записей

### 4.4 Визиты (/visits)
- Комплексная форма создания визитов

- Выбор пациента, врача и кабинета через селекты

- Указание диагноза, состояния и рекомендаций

- Стоимость визита и отметка об оплате

### 4.5 Платежи (/payments)
- Форма создания платежей с выбором визита

- Автоматическое заполнение суммы из стоимости визита

- История платежей с статистикой

- Подсчет общей суммы, наличными и картой

5. Подключение Vuetify
✅ Выполнено:

- Установлен и настроен Vuetify через Vue CLI

- Создана единая тема оформления для всех компонентов

- Использованы компоненты Vuetify:

    - v-card, v-data-table, v-form для структуры
    
    - v-text-field, v-select, v-textarea для форм
    
    - v-btn, v-icon для интерактивных элементов
    
    - v-alert, v-chip для уведомлений и статусов

- Реализован адаптивный дизайн для мобильных устройств

- Настроена кастомная пагинация для таблиц

- Добавлены индикаторы загрузки и обработка ошибок


### Заключение
Разработанная система представляет собой полнофункциональное веб-приложение для управления медицинской клиникой с современным пользовательским интерфейсом, соответствием REST архитектуре и соблюдением всех требований технического задания. Приложение готово к использованию в учебных и демонстрационных целях.